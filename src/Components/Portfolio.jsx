import React from 'react';
import Background from "./iconBackground";
import Navbar from "./Navbar";

export default function Portfolio() {
    return (
        <div style={{display: "flex" ,justifyContent: "center"}} > 
            <Background />
        </div>
    )
}
